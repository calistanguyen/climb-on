import React from 'react';
import LogCard from './LogCard';

const Dashboard = () => {
    return (
        <div className="dashboard">
            <LogCard />
        </div>
    );
}

export default Dashboard