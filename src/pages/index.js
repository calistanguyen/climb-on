import React from 'react';
import LogIn from '../components/LogIn'

//each file in my page directory corresponds to a route. 
const index = () => {
    return (
        <LogIn />
    );
}

export default index; 